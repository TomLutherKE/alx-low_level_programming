#include "main.h"
#include <stdio.h>

/**
 * main - check the code for students.
 *
 * Return: Always 0.
 */

char *_strpbrk(char *s, char *accept)
{
	while (*s != '\0')
		s++;
	while (*accept != '\0')
		accept++;
	return (0);

}	
